import {print, input} from '../../../io_utils.js'

// Leia 2 (duas) frações (numerador e denominador), calcule e escreva a soma destas frações, escrevendo o resultado em forma de fração.

function main() {
    print("------- Somar duas Frações -------\n")

    const fracao1 = Number(input("Insira a 1ª fração: "))
    const fracao2 = Number(input("Insira a 2ª fração: "))

    const soma = 

    print(`\nA soma de ${fracao1} e ${fracao2} é ${soma}`)
}

main()
