import {print, input} from '../io_utils.js'

function main() {
    // entrada
    const a = Number(input("A = "))
    const b = Number(input("B = "))

    // procesamento
    const soma = a + b

    // saída
    print(`SOMA = ${soma}\n`)
}

main()